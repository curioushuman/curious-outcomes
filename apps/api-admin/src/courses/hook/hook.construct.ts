import { Construct } from 'constructs';

/**
 * These are the components required for the api-admin stack
 */
export class CoursesHook extends Construct {
  constructor(scope: Construct, id: string) {
    super(scope, id);

    /**
     * TBD
     * We'll bring some of the chaos from api-admin.stack.ts into here
     */
  }
}
